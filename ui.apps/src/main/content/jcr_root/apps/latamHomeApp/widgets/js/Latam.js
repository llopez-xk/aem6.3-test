/* Namespace definition */
"use strict";

Latam.authoring = (function(v) {
    return v;
}(Latam.authoring || {}));

Latam.authoring.Widgets = (function(v) {
    return v;
}(Latam.authoring.Widgets || {}));